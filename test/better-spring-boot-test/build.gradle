plugins {
    id 'java'
}

version 'io.better'

dependencies {

    compile project(':better-jwt-spring-boot-starter')
    compile project(':better-message-callback-spring-boot-starter')
    compile('org.springframework.boot:spring-boot-starter-web')
}